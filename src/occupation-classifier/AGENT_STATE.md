# Occupation Classifier Agent State

This file is temporary working state for coding agents. Keep durable project invariants in `AGENTS.md`; keep turn-by-turn context here.

## Current Boundary

- `src/occupation-classifier/specialization/**` is the active work area.
- Specialization is a structural extraction framework for concepts and role heads from title/query text.
- Do not change classifier scoring, ranking, family selection, leaf promotion, retrieval, runtime artifacts, or candidate resemblance logic while doing specialization-framework work.
- If specialization changes expose a downstream classifier failure, report it as separate instead of patching across the boundary.

## Current Work

- Current work adds `specializationGate(...).queryWeights.concepts`, a specialization-owned relevance evidence surface for query-side concepts.
- Query concept weights are descriptive data for API callers. They must not encode classifier ranking policy or mutate gate pass/reject decisions.
- Current query concept weights use role-head scoped concept leaf frequency from `specialization-schema/concept-leaf-frequency.json` when the query has a role-head scope, with global concept frequency kept as fallback and tie context.
- Query concept weights expose a lean public shape only: `conceptId`, `dimension`, `values`, and one final `weight`.
- Internally, weights combine role-head scoped frequency with generated ESCO concept-composition evidence. The generator records, per role head, concept sets found in canonical leaf labels and the role-facing head concepts in those sets.
- Query weights are normalized within the query, so single-concept queries return weight `1` and multi-concept queries return relative importance.
- The concept frequency generator now writes `roleHeadLeafFrequency` beside global `leafCountByConceptId`; each role-head entry includes `directLeafCountByConceptId` and `conceptCompositions`.
- `Network Electrical Technician` currently weights `network_work_object=1` and `electrical=0.65` because ESCO role-head composition/direct evidence supports `network` as a technician specialization more directly than `electrical`.
- `Software Embedded Developer` and `Embedded Software Developer` currently produce the same query weights: `software_work_object=1`, `embedded_work_object=0.65`, because ESCO has `embedded systems software developer` with `software_work_object` as the role-facing concept head.
- `Sofware Embedded Developer` does not infer `software`; `sofware` remains unresolved and only `embedded_work_object=1` is weighted. Typo/fuzzy recovery is a separate boundary.
- `npm run classification:specialization -- --query="Network Electrical Technician" --locale=en` now prints a `query weights` section by self-gating the specialization result.
- The intended caller flow is: specialization extracts and gates concepts, the gate returns query-side concept importance, and classifier/application code decides how to apply those weights.
- Role-head phrase vs specialization-concept span arbitration was stabilized in commit `c19063a`.
- Current work adds explicit structural combination evidence and a specialization-owned explain helper.
- Structural combination evidence belongs to the specialization framework output as `structural_combination`; it is not a direct `role_head` alias and should not rewrite concept dimensions.
- The classifier benefits from combination-derived role heads only at the translation boundary in `buildQueryStructuralProfile`, where `structural_combination.derivedRoleHeads` are merged into the classifier-facing `profile.role_head`.
- Reviewed combination mappings currently live in `specialization-structural-combinations.csv`.
- `front_desk -> receptionist` is modeled as a structural combination because it can imply the role in classifier use, but it is not a direct role-head alias.
- There is one output for this evidence: `structural_combination`. Do not add a second sibling output such as `derived_role_head`.
- Role-head phrase matches should participate in the same span conflict ordering as concept candidates.
- Role-head phrase matching is stopword-tolerant and reserves the original token span, because role-head aliases include surfaces like `constatator de daune`.
- Concept alias and phrase-dimension matching are still contiguous-token matching. A broad stopword-tolerant concept pass caused family self-validation regressions for titles like `land-based machinery technician` and `speech and language therapist`.
- Accepted role-head phrases may reserve spans and become `role_head` output.
- Rejected role-head phrases must not become `role_head`, must not affect `roleModes`, and must not block winning concept output.
- Every dumped role-head alias should resolve through the full mapper as a role head. Punctuation-tokenized aliases such as `bo'sun` must not be merged away as duplicates of single-token variants such as `bosun`.
- Query and title surfaces should share the same arbitration shape:
  - `classifySpecializationQuery(...)`
  - `classifySpecializationTitle(...)`
  - `classifySpecializationTitleDetailed(...)`

## Tests To Cover

- Equal-length explicit concept beats role-head phrase.
- Longer role-head phrase beats shorter concept.
- Longer concept beats shorter role-head phrase.
- Losing role-head phrase does not influence role modes.
- Query/title/detailed parity for contested spans.
- Live-schema regressions:
  - `co-pilot` resolves to role head `pilot` with no `co` work object.
  - `front-end developer` resolves `front end` to work object `web`.
  - `front desk receptionist` should not double-count role heads or invent contradictory evidence.
  - Romanian `conducator auto` / `conducător auto` resolves to role head `driver` without leaking `auto`.

## Follow-Up

- Concept aliases with stopwords need a separate structural design. Current snapshot review found many concept aliases whose stopword-compressed form would resolve but whose full stopword form does not.
- Do not solve concept stopword handling by making every concept alias globally stopword-tolerant. That previously broke family self-validation.
- Candidate direction: build an explicit compressed-token matching surface for reviewed/eligible concept aliases, map matches back to original spans, then arbitrate normally with concepts and role heads.
- Concept-to-role-head derivation should be modeled through `structural_combination` evidence, not by reclassifying arbitrary concepts as role heads in classifier scoring.
- `classification:specialization` is the CLI smoke surface for this framework.

## Concept Alias Stopword Audit 2026-09-02

- Source files checked: `specialization-concept-aliases.csv`, `specialization-concept-aliases.ro.csv`, `specialization-concept-aliases.hu.csv`.
- Total concept-alias rows checked: 7,243.
- Alias rows containing mapper stopwords: 390.
- Exact full-surface mapper misses among stopword aliases: 380.
- Stopword aliases whose compressed surface resolves the intended concept cleanly: 154.
- Clean recoveries by locale: global 26, ro 127, hu 1.
- Clean recoveries by dimension: work_object 94, task 30, industry 17, knowledge_domain 5, population 4, channel 3, venue 1.
- Of the 154 clean recoveries, 81 currently emit no concept from the full alias and 73 emit weaker/different partial concepts instead.
- Concrete current gap: `speech and language therapist` emits separate `speech` and `language`; `speech language therapist` emits combined concept `speech_and_language`.
- Concrete current gap: `land based machinery technician` emits `land_based_work_object` plus `machinery_work_object`; `land machinery technician` emits combined concept `land_based_machinery_work_object`.
- Implemented concept matching across stopwords in `collectConceptCandidates` by matching compressed concept parts while reserving the full original input span.
- Current post-fix count: 390 stopword alias rows, 230 full-surface concept misses, 158 clean compressed concept aliases now resolving through the full mapper surface, 4 clean compressed aliases intentionally owned by role-head phrase output, 0 clean compressed aliases still missed as concept when not role-head-owned.
- Post-fix examples: both `speech and language therapist` and `speech language therapist` resolve concept `speech_and_language`; both `land based machinery technician` and `land machinery technician` resolve concept `land_based_machinery_work_object`.
- Added ESCO leaf canonical-label snapshot from `data/runtime-review/occupation-leaf-structure.esco_1_2_1.json`: 3,039 leaves, 2,531 leaves with specialization concepts, 4,288 total concept matches, 1,518 unique concept IDs. The snapshot asserts all emitted concepts exist in the specialization schema and pins source-title witnesses for `speech_and_language` and `land_based_machinery_work_object`.
- Audit conclusion: concept stopword handling was a real specialization-framework issue. The implemented shape keeps matching inside concept candidate collection and uses the existing concept/role-head arbitration loop.

## Structural Combination Review 2026-09-03

- Searched for more `front_desk -> receptionist`-style concept-only structural combinations by grounding candidates in ESCO canonical leaf labels (`data/runtime-review/occupation-leaf-structure.esco_1_2_1.json`), role-head alias files, and the raw job-title review CSVs under `data/taxonomy-review/`.
- Rejected as insufficiently grounded (real titles found for these concepts already carry an explicit role word, so there is no concept-only gap, or the mapping is ambiguous across multiple role heads): `checkout_work_object` (only ESCO evidence is "checkout supervisor", role word already present), `payroll_work_object` (only ESCO evidence is "payroll clerk", but no leaf-frequency or alias evidence ties payroll to clerk specifically), `switchboard_work_object`, `forklift_work_object`, `help_desk`, `warehouse` (all real ESCO leaves found for these concepts already include an explicit role word like operator/agent/manager/clerk/worker, or the concept resolves to multiple plausible role heads with no dominant one).
- Added `housekeeping_task -> housekeeper`: grounded in ESCO canonical leaves "domestic housekeeper" and "housekeeping supervisor"; `Housekeeping` and `Hotel Housekeeping` currently resolve `role_head=none` with no direct role-head alias for the housekeeping task concept. Verified via `npm run classification:specialization -- --query="Housekeeping" --locale=en` before and after (structural combination now emits `derivedRoleHeads=housekeeper`).
- `roleHeadLeafFrequency` in `concept-leaf-frequency.json` has no entries for `front_desk` or `housekeeping_task` under any role head — leaf-frequency evidence is not populated for these combination concepts, so grounding for this file currently comes from ESCO canonical labels / real title evidence, not leaf-frequency data. Future sessions doing this review should not expect leaf-frequency to confirm or refute candidates here.
- Next candidates to investigate if continuing this work: concepts with a single dominant real-world occupation but currently no role-head alias coverage (similar shape to front_desk/housekeeping). Broad grep across `data/taxonomy-review/job-title-raw-sample*.csv` for concept-only titles (no role word) did not surface further clean hits in this pass — a more systematic mining pass (e.g. scripted diff of concept-only titles against role-head vocabulary) would likely be needed to find more at scale.
- Built a scripted miner (not committed, was scratch at `/private/tmp/.../scratchpad/mine-candidates.mjs`) that: for every concept in `specialization-concept-rules.csv` not already in the combinations file and whose canonical is not itself a role head, runs it alone through `classifySpecializationQuery` to confirm `role_head` is empty, regex-searches ESCO canonical leaf labels for that word as a whole token, and buckets matched leaves by which role-head word (if any) each one already carries. Concepts where every role-worded match agrees on the same single role head (no competing role heads at all) are strong candidates; single-leaf-only matches are weaker (could be one ESCO compound, not a reliable real-world pattern) and were mostly skipped. This scales far better than manual grep — surfaced ~34 fully-dominant multi-leaf candidates in one pass.
- Batch-added 2026-09-03 from that miner, each verified role_head=none before and derivedRoleHeads present after, rebuild + full suite run twice (suite has a known flaky unrelated failure pair in `translation.test.js`/`base-vs-specialized-leaf-ranking.test.js` that appears/disappears across runs independent of this file — rerun to confirm before trusting a single red run): `wholesale -> merchant` (36 ESCO leaves, all "wholesale merchant"), `massage_work_object -> therapist`, `teaching -> assistant` (task "teaching", distinct from the already-covered "teacher" role head), `enforcement_task -> officer`, `grinding_task -> operator`, `stitching_task -> operator`, `spinning_task -> operator`, `sewing_machine -> operator`, `surveying_task -> technician` (distinct from the already-covered "surveyor" role head), `deinking_task -> operator`.
- Deliberately skipped from the same miner output as too risky/ambiguous for real-world queries despite passing the ESCO-dominance filter: `account -> manager` (very common word, real-world titles split across manager/executive/accountant), `student -> coordinator` (too broad a bare word), `reception -> operator` (would collide in spirit with the existing `front_desk -> receptionist` combination but for an unrelated industrial-reception sense; the bare word "reception" in real usage skews toward the front-desk sense, not this one), `shift -> manager`, `division -> manager`, `labour -> officer`, `purchasing -> manager` (real-world also uses "purchasing officer"/"buyer"), `printing -> technician`, `coordination -> manager`, `mental -> worker`, `physical -> teacher` (single ambiguous words with broad real-world usage beyond the narrow ESCO evidence). If continuing this work, rerun the miner and work through its full candidate list (`total candidates: 1002` before filtering) rather than re-deriving it by hand.
- **Role-head genericity filter added 2026-09-03**: user feedback pushed back on a large draft batch that leaned heavily on `operator`/`worker`/`technician` derivations (e.g. `boring_task -> operator`, `forging_task -> worker`, `pneumatic_industry -> technician`) — these role heads are catch-alls that apply across dozens of unrelated ESCO leaves, so a combination deriving to one adds far less real disambiguating value than a combination deriving to a role head that is itself close to a distinct occupation (`receptionist`, `housekeeper`, `merchant`, `therapist`, `breeder`, `sommelier`, `pilot`, `judge`). The miner's `total===topCount` dominance filter alone does not capture this — it only proves the ESCO leaves agree, not that the role head is distinctive. Going forward, after the dominance filter passes, also reject candidates whose `topRole` is in the generic set {`operator`, `worker`, `technician`, `manager`, `officer`, `assistant`, `specialist`, `clerk`, `coordinator`, `supervisor`, `analyst`, `administrator`, `representative`, `controller`, `attendant`, `leader`, `expert`, `consultant`, `adviser`} unless there is no viable alternative and the note explicitly justifies the exception (e.g. `enforcement_task -> officer`, `teaching -> assistant` from the round-2 batch were kept as committed history rather than walked back, since amending already-committed rows was judged not worth the git churn — but new batches should default to filtering these out before drafting rows, not after).
- Batch-added 2026-09-03 (second pass, high-value role heads only, replacing a draft that had 15/20 rows on generic operator/worker/technician derivations before commit): `literacy -> teacher`, `performing_task -> instructor`, `drama -> teacher`, `aerodynamics -> engineer`, `addiction_task -> counsellor`, `marriage_work_object -> counsellor`, `bee_work_object -> breeder`, `sheep_work_object -> breeder`, `beer_work_object -> sommelier`, `mountain_work_object -> guide`, `wedding_task -> planner`, `urban_work_object -> planner`, `quantity_work_object -> surveyor` (distinct from the general `surveyor` role head, which needs no help resolving on its own), `naval_industry -> architect`, `helicopter_work_object -> pilot`, `drone_work_object -> pilot`, `shiatsu_work_object -> practitioner`, `speech_and_language -> therapist`, `organ_work_object -> builder`, `supreme_work_object -> judge`. All grounded in a single unambiguous ESCO leaf per concept (e.g. "bee breeder", "wedding planner", "supreme court judge") with no competing role head. Each verified `role_head=none` before / correct `derivedRoleHeads` after via `npm run classification:specialization -- --query="<Concept>" --locale=en`.
- **Discovered a 5th pre-existing test failure in the current dirty working tree, unrelated to this file**: `reviewed stopword-compressed concept aliases resolve through the full mapper surface` in `specialization-dimension-mapper.test.js` now fails (`231 !== 230`) on the full suite. Confirmed via bisection (temporarily swapping the built combinations csv back to the exact round-2-committed 12-row content and rerunning that single test file) that this failure reproduces identically with or without this session's structural-combination additions — it is caused by something else already modified in the dirty working tree at session start (`families.ts`, `family-structure.ts`, `specialization-gate.ts`, `concept-leaf-frequency.json` per `git status`), not by any row in `specialization-structural-combinations.csv`. Treat this as a 5th known-and-currently-pre-existing failure alongside the original 4 until whoever owns those other dirty files investigates it; full suite baseline for this session's work is 503 tests / 468 pass / 5 fail.
- **Multi-concept (`;`-separated) combination batch 2026-09-03, in response to explicit user feedback to favor multi-concept combinations over single-concept ones ("one off concept can be used as modifier")**: the csv/mapper already supported semicolon-separated `concept_ids` (`parseSemicolonList` in `specialization-dimension-mapper.ts`), unused by any prior row. Built a pair-co-occurrence miner (scratch, not committed): for every ESCO leaf label, find all matching concepts, then for every 2-combination co-occurring in that label, tally which role head (if any) it carries, keeping only pairs where every role-worded co-occurrence agrees on one role head. Produced ~3400 raw pairs, filtered to ~529 after deduping by canonical-word-pair, excluding self-referential pairs (concept_id already contains the role word), excluding the same generic-role-head blocklist as the single-concept filter, and excluding ambiguous bare-word canonicals (`at`, `central`, `special`, `policy`, `centre`, `gambling`, `assurance`, `quality`) even as combination components.
- **Critical gotcha found while adding this batch**: a candidate's *mined* `concept_id` is not necessarily what the live mapper actually resolves for that canonical word in context — many canonicals have multiple dimension-variant concept IDs (e.g. `equipment_task` vs `equipment_product` for "equipment"), and phrase-level alias matching can also swallow one of the two intended words into the other concept's span (e.g. querying "specialised video" resolves to only concept `specialised`, not two concepts) or merge them into an unrelated third concept. Any multi-concept row must be built from `classifySpecializationQuery(...).concepts.map(c => c.conceptId)` run on the actual candidate phrase, not from the miner's assumed concept_id — of 50 hand-picked candidates, only 25 survived this correctness check (23 collapsed to fewer than 2 resolved concepts, 2 resolved to a concept_id different from the miner's guess but were still usable once corrected).
- Batch-added 2026-09-03 (25 verified multi-concept rows, all confirmed `role_head=[]` before and correct `derivedRoleHeads` after via `classifySpecializationQuery(...).structural_combination`): `compliance;information;security -> director` (consolidated from 3 separate pairwise candidates that all evidence the single ESCO leaf "director of compliance and information security"), `design;equipment_task -> engineer`, `equipment_task;specialised -> seller`, `business;vocational -> teacher`, `arts;vocational -> teacher`, `technology;vocational -> teacher`, `education_task;physical -> teacher`, `languages;secondary_school -> teacher`, `education_task;secondary_school -> teacher`, `needs_population;school -> teacher`, `school;special_educational_needs -> teacher`, `advertising;sales -> agent`, `railway;sales -> agent`, `sales;ticket_task -> agent`, `automotive;test -> driver`, `bank;central_bank -> governor`, `department;school -> head`, `department;university -> head`, `programme;youth -> director`, `agricultural;design -> engineer`, `airport;planning -> engineer`, `environmental;mining -> engineer`, `design;industrial -> engineer`, `maintenance;repair -> engineer`, `geotechnical_industry;mining -> engineer`.
- **3 candidates rejected mid-mining for lacking real 2-concept co-resolution** worth noting if continuing this work later (they read as plausible pairs but the live mapper only ever resolves one concept, or zero, for the joined phrase, so there is no real structural gap to fill via this csv): "diesel engine" (resolves only `engine_work_object`), "instruments musical" (resolves only `musical`), "beverage food"/"beverage packaging"/"fuel liquid fuel"/"footwear textile" (resolve to 0 or 1 concept). A further ~20 of the originally-picked 50 fell into this same trap (e.g. "specialised video/audio/computer/games" all collapse to just `specialised`; "aviation ground/systems", "container design", "design tool", "distribution gas", "industrial leather", "energy systems/wind", "project rail", "fire prevention" all collapse to a single concept). Re-verify with `classifySpecializationQuery` before drafting, not after.
- **Suite re-verified after this batch with a fresh full bisection (round-2 12-row commit → round-3 34-row → round-3+pairs 59-row)**: the same 8 failures reproduce identically at all three csv states, confirming 3 more failures beyond the previously-known 5 are also pre-existing/unrelated to this csv (not caused by this session): `classifier uses legal industry support to choose legal professionals for compliance coordinator wording`, `exact canonical role-head shortcut yields to a better structurally grounded leaf`, `translateTitleForClassifier lets a role-head alias own a token that also has concept aliases`. Updated baseline: 8 known-and-pre-existing failures, all reproducing with the original committed 12-row csv alone, none introduced by any row added this session.

## Validation

- Started the role-containing concept cleanup on 2026-09-03 with the highest-risk concrete case: replaced `doctor_surgery_work_object` (`doctor surgery`, `work_object`) with `medical_practice` (`medical practice`, `venue`) and moved English/Romanian/Hungarian aliases (`doctor surgery`, `doctors surgery`, `cabinet medical`, `orvosi rendelő`) to that concept. Mapper behavior is now: `Doctors' surgery assistant` => role heads `doctor`, `assistant` plus concept `medical_practice` as `venue`; `doctor` alone remains role-head only; `surgery` alone remains unresolved/no concept.
- Expanded that cleanup to remove the remaining 185 role-containing concept canonicals from live specialization schema and frozen schema fixtures. Post-cleanup audit reports 0 concept canonicals containing canonical role-head/role-alias tokens. The removed rows were title-shaped concept debt; live mapper already decomposed them as role heads plus existing concepts/literals rather than resolving them as their own concepts.
- Removed the remaining 81 exact single-token role-head concepts on 2026-09-03. Selection rule: concept canonical is one token and exactly matches a canonical `role_head` from `specialization-role-heads.csv` (role aliases intentionally excluded). Post-cleanup audit reports 0 direct role-head concept canonicals and 0 embedded role-head concept canonicals.
- Removed aliases/translation rows for those 81 concepts from live specialization schema and frozen schema fixtures. Removed 5 deleted IDs from concept-equivalence groups and 69 additional stale direct structural-combination rows from the frozen fixture snapshot.
- Removed 85 frozen fixture structural-combination rows that referenced deleted title-shaped concepts (all `_direct` combinations such as `liquor_blender_work_object_direct`). Live structural-combination source had no dangling references after the cleanup.
- Updated concept-equivalence groups to remove 5 deleted title-shaped concept IDs: `common_carrier_task`, `database_designer_work_object`, `database_integrator_work_object`, `hand_luggage_work_object`, `practice_educator_industry`.
- Added a narrow mapper rule for role-qualified concept aliases: an alias may include a role-head token only when the concept canonical itself does not contain a role head, and the concept candidate claims only the contiguous non-role token span. This keeps title-shaped concept rows from becoming active while allowing aliases like `doctor surgery` to map to `medical_practice`.
- Added schema integrity test `specialization concepts do not duplicate canonical role heads` so future concept-rule rows cannot duplicate or embed canonical role heads.
- After removing the direct role-head concepts, ESCO leaf concept snapshot is now 3,039 leaves, 2,643 leaves with specialization concepts, 4,657 total concept matches, 1,536 unique concept IDs. Stopword concept-alias snapshot is now 218 rows, 116 full-surface misses, 100 clean compressed aliases resolving on the full surface, 0 owned by role-head-only output, 0 clean compressed aliases missed outside role-head ownership.
- After removing the direct role-head concepts, lean query-weight snapshot is now 3,039 leaves, 2,643 leaves with weights, 1,536 unique weighted concept IDs, 1,365 multi-concept leaves, 1,364 leaves with relative weights.
- Validation note: `npm run test:classifier:specialization` is currently blocked before execution by unrelated TypeScript errors in `decision.test.ts`, `family-validation.test.ts`, and `result.test.ts` (`CanonicalResemblance` test fixtures missing `wildDimensionValues`). Direct compiled specialization tests were run instead.
- `npx tsx --test tests/occupation-classifier/unit/specialization-dimension-mapper.test.ts` passed on 2026-09-03 with 73 passing tests after the `medical_practice` cleanup.
- `npx tsx --test tests/occupation-classifier/unit/specialization-schema-alias-resolution.test.ts` passed on 2026-09-03 with 2 passing tests after updating live schema and frozen schema snapshot aliases.
- `npx tsx --test tests/occupation-classifier/unit/specialization-gate.test.ts` passed on 2026-09-03 with 43 passing tests after regenerating `concept-leaf-frequency.json`; the lean query-weight snapshot is now 3,039 leaves, 2,643 leaves with weights, 1,537 unique weighted concept IDs, 1,366 multi-concept leaves, 1,365 leaves with relative weights.
- `node scripts/occupation-classifier/generate-concept-leaf-frequency.mjs --check` passed on 2026-09-03 with 1,537 concepts over 3,039 leaves.
- `npm run test:classifier:specialization` passed on 2026-09-03 with 73 passing tests after the `medical_practice` cleanup.
- `npm run classification:specialization -- --query="Doctors' surgery assistant" --locale=en --format=json` passed on 2026-09-03 and shows `medical_practice` as venue with query weight `1`.
- `npm run classification:specialization -- --query="specialised video" --locale=en --format=json` passed on 2026-09-03 after fixing fallback-resolved literal concepts to also appear in `concepts[]`; output includes `specialised` as `industry` and `video_work_object` as `work_object`.
- `npx tsx --test tests/occupation-classifier/unit/specialization-dimension-mapper.test.ts` passed on 2026-09-03 with 72 passing tests after the same fix. The ESCO leaf concept snapshot is now 3,039 leaves, 2,642 leaves with specialization concepts, 4,658 total concept matches, 1,536 unique concept IDs.
- `npx tsx --test tests/occupation-classifier/unit/specialization-gate.test.ts` passed on 2026-09-03 with 43 passing tests after regenerating `concept-leaf-frequency.json`. The lean query-weight snapshot is now 3,039 leaves, 2,642 leaves with weights, 1,536 unique weighted concept IDs, 1,367 multi-concept leaves, 1,366 leaves with relative weights.
- `node scripts/occupation-classifier/generate-concept-leaf-frequency.mjs --check` passed on 2026-09-03 with 1,536 concepts over 3,039 leaves.
- `npm run test:classifier:specialization` passed on 2026-09-03 with 72 passing tests after fixing the `specialised video` regression.
- `npx tsx --test tests/occupation-classifier/unit/specialization-gate.test.ts` passed on 2026-09-02 with 39 passing tests after adding `queryWeights.concepts`.
- `node --test dist/tests/occupation-classifier/unit/specialization-gate.test.js` passed on 2026-09-02 with 39 passing tests after adding `queryWeights.concepts`.
- `npm run build:tests` passed on 2026-09-02 after adding `queryWeights.concepts`.
- `npm run test:classifier:specialization` passed on 2026-09-02 with 71 passing tests after adding `queryWeights.concepts`.
- `node scripts/occupation-classifier/generate-concept-leaf-frequency.mjs --check` passed on 2026-09-02 after adding `roleHeadLeafFrequency`.
- Use `npm run test:classifier:specialization` for the fast specialization mapper loop.
- `npm run test:classifier:specialization` passed on 2026-09-02 with 69 passing tests after adding specialization explain coverage and structural-combination stability tests.
- `npm run test:classifier:unit` passed on 2026-09-02 with 424 passing tests after adding specialization explain coverage and structural-combination stability tests.
- `npx tsx --test tests/occupation-classifier/unit/specialization-dimension-mapper.test.ts` passed on 2026-09-02 with 71 passing tests after adding the ESCO leaf canonical-label concept snapshot.
- `npm run test:classifier:specialization` is currently blocked before test execution by unrelated TypeScript errors in dirty `src/occupation-classifier/role-head-groups.ts`.
- `npm run test:classifier:specialization` passed on 2026-09-02 with 62 passing tests after adding single-output structural combination evidence.
- `npm run test:classifier:unit` passed on 2026-09-02 with 417 passing tests after adding single-output structural combination evidence.
- `npm run test:classifier:specialization` passed on 2026-09-02 with 58 passing tests.
- `npm run test:classifier:unit` passed on 2026-09-02 with 412 passing tests.
- `npm run test:classifier:integration` was run on 2026-09-02. It had the expected single failure:
  `base-vs-specialized-leaf-ranking.test.js` expected `leaf`, got `family`.
- Do not run `npm run test:structural` for this task unless explicitly requested.

## Role/Modifier Concept Cleanup Follow-Up 2026-09-03

- Corrected reviewed ambiguous leftovers from the role-shaped concept cleanup:
  - `au pair` is now the canonical role head phrase in `specialization-role-heads.csv`; `pair` was removed as the standalone canonical head. No `au_pair_work_object` concept remains.
  - `stand_up_comedian_work_object` remains removed; added `stand_up_work_object` as the modifier concept (`stand up` / `stand-up`) while `comedian` stays the role head.
  - `sound_designer_work_object` remains removed; no title-shaped aliases like `sound designer`, `sound editor`, `sound mastering engineer`, or `sound operator` were added. `sound designer` now resolves as `sound_knowledge_domain` plus role head `designer`.
  - `common_carrier_task` was restored as a reviewed atomic concept phrase; `carrier` must not become the role head inside `common carrier`.
  - `matrose_work_object` was removed because `matrose` is already a role-head alias for canonical role head `sailor`.
- Mapper fixes in specialization only:
  - Multi-token canonical role heads are indexed as role-head phrase matches using full phrase tokens, so `au pair` is not compressed to `pair`.
  - Stopword-sensitive concept full-surface matching is limited to reviewed semantic phrases (`common_carrier_task`, `stand_up_work_object`) so normal aliases still keep stopwords out of concept output.
  - A longer reviewed concept phrase can cover a canonical role-head token inside its own phrase, allowing `common carrier` to beat standalone `carrier`; role-head aliases like `matrose` still win over stale/removed concept aliases.
- Latest modifier audit over removed concept rows:
  - `removedRows=268`, `rowsWithModifierTokens=183`, `rowsWithoutModifierTokens=85`.
  - Full-title modifier gaps: only `man_made_fiber_spinner_work_object` remains, intentionally left as non-ESCO-derived/noisy (`man made` not useful here; `fiber` is literal only).
  - `common_carrier_task` and `sound_designer_work_object` are covered in full-title context (`common_carrier_task` and `sound_knowledge_domain` respectively), even though their modifier-only fragments alone are intentionally not enough.
- Latest focused validation:
  - `npm run build` passed after these changes.
  - `npx tsx --test tests/occupation-classifier/unit/specialization-dimension-mapper.test.ts` passed with 75 tests.
  - `npx tsx --test tests/occupation-classifier/unit/specialization-gate.test.ts` passed with 43 tests.
  - `node scripts/occupation-classifier/generate-concept-leaf-frequency.mjs --check` passed with 1,536 concepts over 3,039 leaves.
  - Current ESCO leaf specialization snapshot: 3,039 leaves, 2,642 leaves with specialization concepts, 4,658 total concept matches, 1,538 unique concept witnesses.
  - Current lean query-weight snapshot: 3,039 leaves, 2,642 leaves with weights, 1,538 unique weighted concept ids.
