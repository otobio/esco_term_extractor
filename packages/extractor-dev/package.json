{
  "name": "@term-extractor/extractor-dev",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Dev-only embedding-model term extractor (TermExtractor + Embedder) and its calibration/eval tooling. Not used by the production ingest path, which resolves purely via OpenSearch + rule inference.",
  "exports": {
    ".": "./src/index.ts"
  },
  "bin": {
    "term-extractor": "./bin/cli.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@huggingface/transformers": "^3.3.0"
  },
  "devDependencies": {
    "tsx": "^4.19.0",
    "typescript": "^5.6.0",
    "vitest": "^4.0.0"
  }
}
